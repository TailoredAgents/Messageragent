# 20251111_add_agent_runtime_state

Adds a runtime table to track whether the messenger agent is paused per tenant. This powers the dashboard toggle so owners can pause/resume automation without redeploying.
